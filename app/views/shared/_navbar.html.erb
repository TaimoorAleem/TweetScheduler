<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <div class="d-flex align-items-center">
      <% if @user %>
        <div class="me-3">Signed in as: <%= @user.email %></div>
        <%= button_to "Sign Out", signout_path, method: :delete, class: "btn btn-outline-danger py-1" %>
      <% else %>
        <%= link_to "Sign In", signin_path, class: "btn btn-outline-primary me-2" %>
        <%= link_to "Register", register_path, class: "btn btn-outline-secondary" %>
      <% end %>
    </div>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "About", about_path, class: "nav-link" %>
        </li>
      </ul>
    </div>
  </div>
</nav>